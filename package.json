{
    "name": "generic-clinic-automation",
    "version": "1.0.0",
    "description": "Multi-tenant clinic booking automation platform",
    "scripts": {
        "build": "cd platform/widget && npm install && npm run build && cp -r functions dist/",
        "dev": "cd platform/widget && npm run dev"
    },
    "private": true
}